local RaelHubFunctionCards = {}

function RaelHubFunctionCards.ShowCards(Key)
   if Key == "85791345" then
    local RaelHubEspDestaque = loadstring(game:HttpGet"https://raw.githubusercontent.com/Raelhub/Rael-hub-function/main/function/createesp/No%20Hitbox%20destaque/script.txt")()
    
    local Carta1 = "rbxassetid://10981758963"
    local Carta2 = "rbxassetid://10981758963"
    local Carta3 = "rbxassetid://10981758963"
    local Carta4 = "rbxassetid://10981758963"
    local Carta5 = "rbxassetid://10981758963"
    local Carta6 = "rbxassetid://10981758963"
    local CartaPosition1 = 1
    local CartaPosition2 = 2
    local CartaPosition3 = 3
    local CartaPosition4 = 4
    local CartaPosition5 = 5
    local CartaPosition6 = 6
    
    -- Função para criar uma carta
    local function criarCarta(assetId, texto)
        local carta = Instance.new("ImageLabel")
        carta.Size = UDim2.new(0, 75, 0, 100) -- Tamanho das cartas
        carta.Image = assetId -- Definindo a imagem da carta
        carta.BackgroundTransparency = 1 -- Tornando o fundo transparente
    
        -- Criando a label de texto
        local textoLabel = Instance.new("TextLabel")
        textoLabel.Size = UDim2.new(1, 0, 1, 0) -- Preencher a carta
        textoLabel.Position = UDim2.new(0, 0, 0, 0) -- Alinhamento dentro da carta
        textoLabel.BackgroundTransparency = 1 -- Fundo transparente
        textoLabel.Text = texto -- Definindo o texto da carta
        textoLabel.TextColor3 = Color3.fromRGB(47, 236, 154) -- Cor do texto (verde)
        textoLabel.TextStrokeTransparency = 0.6 -- Borda do texto
        textoLabel.TextSize = 20 -- Tamanho do texto ajustado para 20 pixels
        textoLabel.TextStrokeColor3 = Color3.fromRGB(47, 236, 154) -- Cor da borda do texto (verde)
        textoLabel.TextScaled = false -- Desabilita o ajuste automático do tamanho do texto
        textoLabel.TextWrapped = true -- Quebra o texto se necessário
    
        -- Adicionando a label de texto à carta
        textoLabel.Parent = carta
    
        return carta
    end
    
    -- Criando a interface
    local screenGui = Instance.new("ScreenGui")
    screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
    
    -- Criando o texto centralizado "Rael Hub"
    local textoCentralizado = Instance.new("TextLabel")
    textoCentralizado.Size = UDim2.new(1, 0, 0, 50) -- Ajuste a altura conforme necessário
    textoCentralizado.Position = UDim2.new(0.5, 0, 0, 0) -- Centralizado no topo
    textoCentralizado.AnchorPoint = Vector2.new(0.5, 0) -- Ajuste para centralizar o texto
    textoCentralizado.BackgroundTransparency = 1 -- Fundo transparente
    textoCentralizado.Text = "Rael Hub"
    textoCentralizado.TextColor3 = Color3.fromRGB(47, 236, 154) -- Cor do texto (verde)
    textoCentralizado.TextSize = 30 -- Tamanho do texto ajustado para 30 pixels
    textoCentralizado.TextStrokeTransparency = 0.6 -- Borda do texto
    textoCentralizado.TextStrokeColor3 = Color3.fromRGB(47, 236, 154) -- Cor da borda do texto (verde)
    textoCentralizado.TextScaled = false -- Desabilita o ajuste automático do tamanho do texto
    textoCentralizado.TextWrapped = true -- Quebra o texto se necessário
    textoCentralizado.Parent = screenGui
    
    local cartasContainer = Instance.new("Frame")
    cartasContainer.Size = UDim2.new(0, 450, 0, 100) -- Largura ajustada para acomodar as cartas menores
    cartasContainer.Position = UDim2.new(0.5, -225, 0, 50) -- Centralizado abaixo do texto "Rael Hub"
    cartasContainer.BackgroundTransparency = 1 -- Tornando o fundo transparente
    cartasContainer.Parent = screenGui
    
    -- Botão para fechar a interface
    local fecharButton = Instance.new("TextButton")
    fecharButton.Size = UDim2.new(0, 50, 0, 50) -- Tamanho do botão
    fecharButton.Position = UDim2.new(1, -50, 0, 0) -- Posição no canto superior direito
    fecharButton.AnchorPoint = Vector2.new(1, 0) -- Ajuste para alinhar no canto
    fecharButton.BackgroundColor3 = Color3.fromRGB(168, 34, 34) -- Cor de fundo alterada para #A82222
    fecharButton.BorderSizePixel = 0 -- Remove o contorno preto
    fecharButton.Text = "X" -- Texto do botão
    fecharButton.TextColor3 = Color3.fromRGB(255, 255, 255) -- Cor do texto (branco)
    fecharButton.TextSize = 24 -- Tamanho do texto
    fecharButton.Parent = screenGui
    
    -- Função para fechar a interface quando o botão for clicado
    fecharButton.MouseButton1Click:Connect(function()
        screenGui:Destroy()
    end)
    
    -- Usando Layout para alinhar as cartas
    local layout = Instance.new("UIListLayout")
    layout.FillDirection = Enum.FillDirection.Horizontal
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Left
    layout.Parent = cartasContainer
    
    -- Adicionando as cartas ao container com texto
    local carta1 = criarCarta(Carta1, CartaPosition1)
    carta1.Parent = cartasContainer
    
    local carta2 = criarCarta(Carta2, CartaPosition2)
    carta2.Parent = cartasContainer
    
    local carta3 = criarCarta(Carta3, CartaPosition3)
    carta3.Parent = cartasContainer
    
    local carta4 = criarCarta(Carta4, CartaPosition4)
    carta4.Parent = cartasContainer
    
    local carta5 = criarCarta(Carta5, CartaPosition5)
    carta5.Parent = cartasContainer
    
    local carta6 = criarCarta(Carta6, CartaPosition6)
    carta6.Parent = cartasContainer
    
    for i = 1, 6 do
      local Object = workspace.Section2.Objective.Cards:GetChildren()[i]
      RaelHubEspDestaque.EspBixo(Object)
      wait(0.1)
    end
    else
      for i = 1, 1000 do
        print("Parado, você quer usar uma função do Rael Hub sem permisão?")
      end  
    end
end

return RaelHubFunctionCards