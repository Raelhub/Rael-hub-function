local AlancasSistemas = {}

function AlancasSistemas.EncontrarPastaAlavancas()
    for _, pasta in ipairs(Workspace:GetChildren()) do
        if pasta:IsA("Folder") then
            for _, model in ipairs(pasta:GetChildren()) do
                if model:IsA("Folder") and model.Name == "HANDLER" then
                    print("Working...")
                    return pasta
                end
            end
        end
    end
end

function AlancasSistemas.AtivarAlanvas(folder)
    for _, alavanca in ipairs(folder.Levers:GetChildren()) do
        if alavanca.Lever.Transparency == 0 then
            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(alavanca.Lever.Position)
            wait(0.5)
            local proximityPrompt = alavanca.Lever:FindFirstChildOfClass("ProximityPrompt")
            if proximityPrompt then
                fireproximityprompt(proximityPrompt)
            end
        end
    end
end

return AlancasSistemas