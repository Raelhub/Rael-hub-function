local EspItens = {}

function EspItens.EspItem(itens, cor)
    local highlight = Instance.new("Highlight")
    highlight.Adornee = monster
    highlight.FillColor = Color3.fromRGB(cor) -- Cor #EAC810
    highlight.Parent = itens
end

return EspItens