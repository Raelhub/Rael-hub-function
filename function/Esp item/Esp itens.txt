local EspItens = {}

function EspItens.EspItem(itens, cor)
    local highlight = Instance.new("Highlight")
    highlight.Adornee = itens -- Troquei de 'monster' para 'itens'
    highlight.FillColor = cor -- A cor já deve ser passada como Color3
    highlight.Parent = itens
end

return EspItens